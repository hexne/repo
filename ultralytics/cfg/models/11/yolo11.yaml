# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics YOLO11 object detection model with P3/8 - P5/32 outputs
# Model docs: https://docs.ultralytics.com/models/yolo11
# Task docs: https://docs.ultralytics.com/tasks/detect

# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolo11n.yaml' will call yolo11.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] # summary: 181 layers, 2624080 parameters, 2624064 gradients, 6.6 GFLOPs
  s: [0.50, 0.50, 1024] # summary: 181 layers, 9458752 parameters, 9458736 gradients, 21.7 GFLOPs
  m: [0.50, 1.00, 512] # summary: 231 layers, 20114688 parameters, 20114672 gradients, 68.5 GFLOPs
  l: [1.00, 1.00, 512] # summary: 357 layers, 25372160 parameters, 25372144 gradients, 87.6 GFLOPs
  x: [1.00, 1.50, 512] # summary: 357 layers, 56966176 parameters, 56966160 gradients, 196.0 GFLOPs

# YOLO11n backbone
backbone:
  - [-1, 1, MLP, [64]]                  # 0: 输入建模 → [B, 64, H, W]
  - [-1, 1, Conv, [128, 3, 2]]             # 1: 下采样 → [B, 128, H/2, W/2]
  - [-1, 1, Conv, [128, 3, 2]]            # 2: 空间建模 → [B, 128, H/2, W/2]
  - [-1, 2, C3k2, [256, False, 0.25]]      # 3: 局部语义 → [B, 256, H/2, W/2]
  - [-1, 1, Conv, [256, 3, 2]]             # 4: 下采样 → [B, 256, H/4, W/4]
  - [-1, 2, C3k2, [512, False, 0.25]]      # 5: 中层语义 → [B, 512, H/4, W/4]
  - [-1, 1, Conv, [512, 3, 2]]             # 6: 下采样 → [B, 512, H/8, W/8]
  - [-1, 2, C3k2, [512, True]]             # 7: 深层语义 → [B, 512, H/8, W/8]
  - [-1, 1, Conv, [1024, 3, 2]]           # 8: 深层空间建模 → [B, 1024, H/8, W/8]
  - [-1, 2, C3k2, [1024, True]]            # 9: 高语义融合
  - [-1, 1, SPPF, [1024, 5]]               # 10: 多尺度融合
  - [-1, 2, C2PSA, [1024]]                 # 11: 注意力增强

# YOLO11n head
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 7], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2, [512, False]] # 13

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 5], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, C3k2, [256, False]] # 16 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 14], 1, Concat, [1]] # cat head P4
  - [-1, 2, C3k2, [512, False]] # 19 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 11], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2, [1024, True]] # 22 (P5/32-large)

  - [[17, 20, 23], 1, Detect, [nc]] # Detect(P3, P4, P5)